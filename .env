# Controls which environment file Docker Compose loads.
# Change this value to switch environments: development | qa | production | local
# use local to use with out docker but make sure redis and postgres is running in your system or in docker with port expose
APP_ENV=local
PORT=3003
